spring.application.name=webproject
server.port=8080

# Disable Docker Compose support
spring.docker.compose.enabled=false

# PostgreSQL Database Configuration
# ✅ FIX: Set timezone in JDBC URL to prevent "Asia/Saigon" detection from Windows
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Ho_Chi_Minh
spring.datasource.url=jdbc:postgresql://localhost:5432/cps_db?TimeZone=Asia/Ho_Chi_Minh
spring.datasource.username=cps_user
spring.datasource.password=cps_pass
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# Multipart (nếu cần upload file, giữ lại; nếu không thì bỏ luôn)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

logging.level.org.springframework.security=INFO
logging.level.org.springframework.security.web=INFO
logging.level.com.proj.webprojrct.common.config.security=DEBUG
logging.level.org.springframework.web=WARN
logging.level.org.springframework.boot.autoconfigure=WARN
logging.level.org.hibernate=WARN


# Error handling
server.error.whitelabel.enabled=false
server.error.include-stacktrace=never
server.error.include-message=always
server.error.path=/error

# Static resources configuration
spring.mvc.static-path-pattern=/resources/**,/img/**,/css/**,/js/**,/images/**
spring.web.resources.static-locations=classpath:/static/
spring.web.resources.cache.period=0

# Enable Thymeleaf
spring.thymeleaf.enabled=true
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
# Static resource configuration

# JSP Configuration is removed as we're now using Thymeleaf
# This line was removed to avoid duplicate logging configuration

# JWT Configuration with Cookie Security
jwt.secret=4i6R5XeUs9zbWUKowvAyDLcuPCpS7GMfZh82QBTjaN3Jdnr0EkOlgF1IxVtHmqYbC3L5vJw8RfTh2o1Znp6DcXaQuE7sKyG9Iv0PdNjM4BW
jwt.access-token-expiration.ms=36000000
jwt.refresh-token-expiration.ms=86400000

# 🍪 Cookie Security Settings
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=strict

# ====================================
# Email Configuration (Gmail SMTP)
# ====================================
# Replace with your actual Gmail credentials
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=lamssdd910@gmail.com
spring.mail.password=fohfttcduwxlpjhk

# SMTP Authentication & TLS
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Email Encoding
spring.mail.default-encoding=UTF-8

# ====================================
# Swagger UI / OpenAPI Configuration
# ====================================
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true
springdoc.swagger-ui.filter=true

# Base URL
app.base-url=http://localhost:8080

# OAuth2 Configuration
spring.security.oauth2.client.registration.google.client-id=your-google-client-id
spring.security.oauth2.client.registration.google.client-secret=your-google-client-secret
spring.security.oauth2.client.registration.google.scope=email,profile

spring.security.oauth2.client.registration.facebook.client-id=your-facebook-app-id
spring.security.oauth2.client.registration.facebook.client-secret=your-facebook-app-secret
spring.security.oauth2.client.registration.facebook.scope=email,public_profile

# Add to application.properties
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# Disable schema initialization (no data.sql file)
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=never

# Don't rely on schema locations - use the default classpath locations
# spring.sql.init.schema-locations=classpath:schema.sql
# spring.sql.init.data-locations=classpath:data.sql
