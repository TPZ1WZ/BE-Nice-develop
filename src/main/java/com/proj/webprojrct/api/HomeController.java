package com.proj.webprojrct.api;

import org.springframework.stereotype.Controller;

/**
 * This controller has been consolidated with ViewController.
 * See com.proj.webprojrct.config.ViewController for the active mappings.
 */
@Controller
public class HomeController {
    // Controller methods have been moved to ViewController to avoid duplicate mappings
}